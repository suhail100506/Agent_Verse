# fake_certificate_verification_agent Flow

This project defines a declarative CrewAI Flow in `src/fake_certificate_verification_agent/flow.yaml`.

## Install

```bash
crewai install
```

## Run

```bash
crewai run
```

Edit the declarative flow definition at `src/fake_certificate_verification_agent/flow.yaml` to change the flow. Add reusable crews under `src/fake_certificate_verification_agent/crews/`, custom Python tools under `src/fake_certificate_verification_agent/tools/`, and shared knowledge files under `src/fake_certificate_verification_agent/knowledge/`.
